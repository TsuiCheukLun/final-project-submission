// BMI Model
class BMI {
  final int id; // ID of the BMI
  final double bmi; // BMI
  final double height; // Height in centimeters
  final double weight; // Weight in kilograms
  final DateTime date; // Date

  BMI({this.id, this.bmi, this.height, this.weight, this.date}); // Constructor
}
